const initialState = {
  name: '',
  phone: '',
  shift: '',
};

const EmployeeDetailsStore = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default EmployeeDetailsStore;
